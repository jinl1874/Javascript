test_obj = {
    name: "apple",
    year: 1999,
    eat_or_not: true
}

for (let index = 0; index < array.length; index++) {
    const element = array[index];

}

function test(params) {
    alert(params);
}